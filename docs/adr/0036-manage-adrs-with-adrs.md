# 0036 — Manage ADRs with adrs

Date: 2026-08-03

## Status

Accepted

## Context

`docs/adr/` was hand-managed: no format validation, no duplicate detection, and numbering by convention only.
Two parallel branches both allocated 0033 on 2026-07-29 and the duplicate landed on `main`. Cross-branch number
collisions are structurally likely as long as numbers are allocated locally (max + 1).

## Decision

- ADRs are managed with the `adrs` CLI (crates.io, pinned 0.10.1 in CI; `cargo install adrs --locked` locally) in
  adr-tools-compatible mode: `adrs.toml` (`adr_dir = "docs/adr"`) + `.adr-dir`; plain markdown, no `--ng`
  frontmatter. New ADRs are created with `adrs new "Title"` — numbers are never hand-allocated.
- Numbering stays sequential and human-readable. Gaps are accepted (0014 is permanent; doctor rule ADR011 is
  suppressed in `adrs.toml`); duplicates are fatal (doctor rule ADR012).
- Enforcement is layered. A pure-shell guard (`ci/verify_adr_numbers.sh`) needs no binary and hard-fails in
  `pre-commit` (including the concluding commit of a conflicted merge), `pre-merge-commit`, and `pre-rebase`
  (which predicts the post-rebase collision from `upstream..branch` and refuses the rebase). `post-rewrite` warns
  only — git ignores its exit code. `adrs doctor` warn-skips locally when the binary is absent and is enforced
  unconditionally in CI's `markdown` job on every push and PR; the push leg is the authoritative cross-branch
  collision gate. Local hooks are latency reduction, not the guarantee — they are bypassable (`--no-verify`) and
  absent until husky-rs installs them on first `cargo build`.
- `docs/adr/README.md` is generated by `scripts/regen-adr-toc.sh` (filename-keyed links; `adrs generate toc` was
  rejected because it links by title slug, which breaks on this corpus's pre-existing filenames) and is
  staleness-checked by the same script's `--check` mode.
- If two branches still mint the same number (merged out of order), CI on the second goes red: the later lander
  renumbers with `git mv` to the next free number and re-points references.

## Consequences

- `adrs doctor` exits 0 across the corpus after a mechanical header normalization (titles, `Date:` lines,
  `## Status` sections, umbrella headings). Residual doctor warnings are accepted: the ADR011-suppressed 0014 gap
  and ~14 ADR014 thin-section warnings — fixing those means writing content, deliberately out of scope.
- The legacy corpus keeps its filenames; titles keep standard technical notation (no slug coupling).
- Tooling risk is bounded: the collision guard and TOC gate are pure shell; only format validation depends on the
  external binary, and an adrs install outage in CI blocks merges the same way a RustSec advisory-DB outage
  already blocks `cargo deny`.
