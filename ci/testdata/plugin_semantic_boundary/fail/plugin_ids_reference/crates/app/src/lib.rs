use uptrakit_shared_types::plugin_type_id::plugin_ids::{self as ids, GENERIC_SHELL};
use uptrakit_shared_types::plugin_type_id::plugin_ids as id_catalog;
use uptrakit_shared_types::plugin_type_id::plugin_ids as ids2;
use ids2 as ids3;
use ids3::{WEBHOOK as HOOK};

pub fn demo() {
    let _ = plugin_ids::GENERIC_SHELL;
    let _ = plugin_ids :: GENERIC_SHELL;
    let _ = ids::GENERIC_SHELL;
    let _ = id_catalog::WEBHOOK;
    let _ = GENERIC_SHELL;
    let _ = ids3::WEBHOOK;
    let _ = HOOK;
}
