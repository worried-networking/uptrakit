# Fixture Notes

This documentation tree mentions `releases_github`, `generic_shell`, and `infrastructure_proxmox` in prose.

```rust
use crate::plugin_type_id::plugin_ids::{GENERIC_SHELL, INFRASTRUCTURE_PROXMOX, RELEASES_GITHUB};
```
