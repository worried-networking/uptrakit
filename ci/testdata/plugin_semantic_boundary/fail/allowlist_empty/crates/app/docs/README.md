# Fixture Notes

This repository mentions `package_manager_apt`, `webhook`, and `releases_github` in documentation only.

```rust
use crate::plugin_type_id::plugin_ids::{PACKAGE_MANAGER_APT, RELEASES_GITHUB, WEBHOOK};
```
