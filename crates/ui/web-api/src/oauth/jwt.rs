// stub — concrete implementation lands in a subsequent commit (Task 4)

/// Signs MCP OAuth access tokens and authorization codes.
///
/// Stub placeholder; replace with the real implementation in Task 4.
#[derive(Clone)]
pub struct McpOAuthJwtSigner;

/// Verifies MCP OAuth access tokens.
///
/// Stub placeholder; replace with the real implementation in Task 4.
#[derive(Clone)]
pub struct McpOAuthJwtVerifier;
